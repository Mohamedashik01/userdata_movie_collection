# userdata_movie_collection
A resilient, offline-first Flutter app for movie discovery and user management. It uses ReqRes.in and Drift (SQLite) to merge remote trends with local data. Features include infinite scroll pagination, user-pinned movie bookmarks, and a custom retry engine with exponential backoff to handle simulated 30% network failures seamlessly.
